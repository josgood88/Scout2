using System;
using Xunit;
using Scout2.Utility;

namespace Scout.Tests {
   public class TestMiscUtils {
      //=====================IsEven=====================
      // IsEven isn't unit tested.  It is simple enough that inspection is deemed sufficient.

      //=====================IsOdd=====================
      // IsOdd isn't unit tested.  It is simple enough that inspection is deemed sufficient.
   }
}
